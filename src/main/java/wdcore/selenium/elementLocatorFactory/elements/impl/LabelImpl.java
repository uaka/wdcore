package wdcore.selenium.elementLocatorFactory.elements.impl;

import org.openqa.selenium.WebElement;

import wdcore.selenium.elementLocatorFactory.elements.Label;



class LabelImpl extends AbstractElement implements Label {
    protected LabelImpl(final WebElement wrappedElement) {
        super(wrappedElement);
    }

//    @Override
   
}
